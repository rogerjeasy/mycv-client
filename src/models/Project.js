class Project {
    constructor(title, projectLink, githubLinks, techStack, tasks) {
      this.title = title;
      this.projectLink = projectLink;
      this.githubLinks = githubLinks;
      this.techStack = techStack;
      this.tasks = tasks;
    }
  }
  
  export default Project;